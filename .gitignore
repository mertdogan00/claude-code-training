# runtime artifacts
node_modules/

# per-app SQLite files are created on first run and recreated on each start
**/*.sqlite
**/*.sqlite3
**/*.db
**/*.sqlite-journal
**/*.sqlite-wal
**/*.sqlite-shm

# the live-build folder created on stage
demo/
