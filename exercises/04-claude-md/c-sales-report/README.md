# C · Satış raporu

Bu egzersizin verisi deponun örnek CSV dosyası, `../../../data/sales-data.csv`: Ağustos
2026'dan 120 satır, sütunlar `date,product,category,qty,unit_price,city`.

Kurallar dosyasından önce ve sonra şunu sor:

```
../../../data/sales-data.csv dosyasındaki satışları şehir şehir özetle.
```

Akışın tamamı (sor, `mv rules.md CLAUDE.md`, `/clear`, tekrar sor, `mv CLAUDE.md rules.md`)
üst klasördeki `../README.md` dosyasında. Örnek cevaplar `expected/` içinde.
